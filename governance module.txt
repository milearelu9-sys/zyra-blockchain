class GovernanceModule:
    def __init__(self):
        self.proposals = []  # List of proposals

    def submit_proposal(self, proposer, description):
        self.proposals.append({'proposer': proposer, 'desc': description, 'votes': {}})

    def vote(self, proposal_id, voter, vote):
        self.proposals[proposal_id]['votes'][voter] = vote

    def tally_votes(self, proposal_id):
        # Count votes and decide outcome
        pass